# API server
PORT=3000

# Serial / UART (ESP32-H2 ot-br CLI)
# Linux: /dev/ttyUSB0 hoặc /dev/ttyACM0
# macOS: /dev/cu.usbserial-*
# Windows: COM3
SERIAL_PORT=/dev/ttyUSB0
SERIAL_BAUD_RATE=115200

# Timeout (ms) chờ output sau khi gửi lệnh CLI
CLI_TIMEOUT_MS=5000
