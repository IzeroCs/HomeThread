# Dashboard backend SQLite (runtime; migrations under data/migrations/ are tracked)
data/*
# Dependencies
node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment (root `.env` is committed for default dev ports; keep overrides local)
.env.local
.env.*.local

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Vite
frontend/dist/

# Backend
backend/dist/

# Local docs symlink (trỏ về namorix-thread/documents/)
docs
