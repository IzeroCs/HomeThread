{
  "name": "thread-dashboard-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:addon": "vite build --config vite.addon.config.ts",
    "build:addon:watch": "vite build --watch --config vite.addon.config.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.9.0",
    "lit": "^3.2.0",
    "shared": "file:../shared",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "sass": "^1.83.0",
    "typescript": "~5.6.3",
    "vite": "^6.0.3"
  }
}
