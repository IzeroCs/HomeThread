# TODO – Namorix Thread

Những phần đã triển khai xem tại `README.md` và `documents/README.md`.

## Backend

- [ ] Bảo mật runtime (Auth API/WebSocket, HTTPS, IP restriction) nếu triển khai môi trường rộng hơn LAN dev.

## Frontend

- [ ] Shortcut lệnh cho thao tác thường dùng.
- [ ] Lịch sử lệnh (nếu khôi phục console/terminal workflow).
- [ ] Live terminal realtime (nếu cần quay lại use case UART-level debug).

## Tích hợp & vận hành

- [ ] Tính năng tìm BR (mDNS trên host, fallback scan TCP 5000 khi chạy Docker).
- [ ] Bổ sung tài liệu frontend Lit: light DOM, SCSS import, modal `.body` pattern.

## Tham chiếu

- `documents/coap/device_payload_spec.md`
- `documents/coap/backend_discovery_srp.md`
- `documents/architecture/real_br_integration.md`
